# macOS で `brew bundle --file=Brewfile` 経由でインストールされる
# パッケージ一覧。chezmoi の run_once_before_install-packages.sh.tmpl から呼ばれる。

# シェル・ターミナル基盤
brew "git"
brew "zsh"
brew "tmux"
brew "neovim"

# CLI ユーティリティ (zshrc / aliases で参照)
brew "fzf"
brew "ripgrep"
brew "fd"
brew "bat"
brew "eza"
brew "zoxide"
brew "atuin"

# プロンプト
brew "starship"

# バージョン管理 (zshrc で `mise activate zsh` 使用)
brew "mise"

# タスクランナー (justfile を実行)
brew "just"

# GitHub CLI
brew "gh"

# ターミナル (macOS のメインターミナル)
cask "wezterm"

# フォント (kitty.conf / WezTerm で使用)
cask "font-monaspice-nerd-font"
