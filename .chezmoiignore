# keyd 設定は /etc/keyd へ run_onchange スクリプトでコピーするため、
# ~/.config/keyd には配置しない（keyd は /etc/keyd/*.conf のみ読む）。
.config/keyd

# Firefox / Zen の設定はランダムなプロファイル名のディレクトリへ
# run_onchange スクリプトでコピーするため、~/.config 直下には配置しない
# （dot_config/firefox・dot_config/zen はあくまで素材置き場）。
.config/firefox
.config/zen
